<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO" monitorInterval="30">
    <Properties>
        <Property name="log.path">logs</Property>
        <Property name="log.pattern">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
    </Properties>

    <Appenders>

        <!-- Console Output -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log.pattern}" />
        </Console>

        <!-- Main Rolling File Log -->
        <RollingFile name="RollingFile" fileName="${log.path}/framework.log"
                     filePattern="${log.path}/archived/framework-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${log.pattern}" />
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
                <SizeBasedTriggeringPolicy size="20 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Error-Only Log -->
        <RollingFile name="ErrorFile" fileName="${log.path}/error.log"
                     filePattern="${log.path}/archived/error-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${log.pattern}" />
            <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
        </RollingFile>

        <!-- JSON Log (Optional, useful for CI/CD or ELK) -->
        <RollingFile name="JsonLogs" fileName="${log.path}/json/framework.json"
                     filePattern="${log.path}/json/framework-%d{yyyy-MM-dd}-%i.json.gz">
            <JsonLayout compact="true" eventEol="true"/>
            <Policies>
                <TimeBasedTriggeringPolicy interval="1"/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
        </RollingFile>

        <!-- Async Logging (improves performance) -->
        <Async name="AsyncAppender" includeLocation="true">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
            <AppenderRef ref="ErrorFile"/>
            <AppenderRef ref="JsonLogs"/>
        </Async>

    </Appenders>

    <Loggers>
        <!-- For your packages -->
        <Logger name="com.yourcompany" level="debug" additivity="false">
            <AppenderRef ref="AsyncAppender"/>
        </Logger>

        <!-- Default root logger -->
        <Root level="info">
            <AppenderRef ref="AsyncAppender"/>
        </Root>
    </Loggers>
</Configuration>
